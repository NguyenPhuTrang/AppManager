const routes = {
  productManager: "/product",
  usertManager: "/user",
  loginManager: "/login",
  registerManager: "/register",
  notFoundPage: "/*",
};
const config = {
  routes,
};

export default config;
