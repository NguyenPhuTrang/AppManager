const routes = {
  productManager: "/product",
  usertManager: "/user",
  loginManager: "/login",
  registerManager: "/register",
  settingManager: "/setting",
  notFoundPage: "/*",
};
const config = {
  routes,
};

export default config;
